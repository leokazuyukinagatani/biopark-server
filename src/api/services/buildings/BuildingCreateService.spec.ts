import { InMemoryBuildingsRepository } from '@test/repositories/in-memory-buildings-repository'

import { BuildingCreateService } from './BuildingCreateService'

describe('Create building',() => {
  it('should be able to create a building', async () => {
    const buildingsRepository = new InMemoryBuildingsRepository()

  })
})